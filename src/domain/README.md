# Domain Modules

Domain code should stay independent from React where practical. Chess rules, curriculum validation, tutor decisions, and progress scheduling live here as the app grows.

The tutor engine evaluates legal moves against curriculum nodes and returns structured feedback for the UI to render.

